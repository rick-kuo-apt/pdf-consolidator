# PDF Consolidator - Requirements
# Core dependencies
PySide6>=6.5.0
pypdf>=3.0.0

# Optional: for page count detection (graceful fallback if not installed)
# PyMuPDF>=1.23.0

# Development/testing
# pytest>=7.0.0
# pyinstaller>=6.0.0
